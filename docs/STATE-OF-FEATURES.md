# State of Features — current main + wave-9

Quick, no-hedge answers to "does this thing actually work in *my* setup?"
Most of the answers below are tied to a code path or a docs file you can read
end-to-end; the few rows that can't be sourced inline are explicitly flagged
**Verify with maintainer** rather than guessed.

> **Maintenance intent:** This file is updated on every release. If you find a
> row that's stale, the right place to flag it is in a PR against this file;
> do not edit the underlying behavior silently.

## Tested in main (wave-9 source audit)

| Feature                                               | Status | Where it's wired / verified                                                                                             |
|-------------------------------------------------------|--------|--------------------------------------------------------------------------------------------------------------------------|
| Spotify Premium ↔ Teams status (work/school)            | ✅     | Spotify calls hit `accounts.spotify.com/api/token` (Authorization Code + PKCE, confidential client) + `/v1/me/player/currently-playing`; Teams calls hit `graph.microsoft.com/v1.0/me/presence/setStatusMessage`. The OAuth rounds are wired in `src-tauri/src/spotify.rs` and `src-tauri/src/teams.rs`. |
| Sign-in survives a long idle period (Dashboard, not the wizard) | ⚠ Partial | `src-tauri/src/commands/onboarding.rs::session_verdict` + `spotify_session_verdict` / `teams_session_verdict` — the launch gate refreshes a locally-expired access token (CAS + persist) instead of probing the API with the stale bearer, and only `invalid_grant` / missing credentials ask for a sign-in again (#530); `src/lib/utils/boot.ts::bootView` routes an incomplete-but-configured install to Reconnect. Decision table + routing are unit-tested; the refresh leg needs one real relaunch after > 1 h with the app closed to be confirmed end to end. Reconnect only auto-starts the Spotify flow when `SyncStatus.spotify_connected` is false (`src/lib/utils/reconnect.ts::shouldAutoStartSpotifyReconnect`, #530 follow-up), so a Teams-only re-auth no longer opens an unrequested Spotify window.  **4.7.0 smoke: not run** — it needs a signed-in install and a > 1 h wall-clock relaunch; the machine used for this release has no desktop session and no Spotify/Teams credentials. The published 4.7.0 artifacts were exercised instead (startup, `--status`, `--sync-once` exit codes) and the decision table is unit-tested; this row stays ⚠ until a maintainer runs the two steps above on a signed-in machine. |
| Smart sleep between polls                              | ✅     | `src-tauri/src/polling/poll_once.rs` — sleeps until `track.duration_ms − track.progress_ms − 5000ms`, clamped to user-configured `min/max_interval_seconds`. ~240 s of silence per 4-min song. |
| Pause-aware backoff (Spotify idle)                     | ✅     | `src-tauri/src/polling/poll_once.rs::pause_backoff(consecutive_pauses, default_secs, ceiling_secs)` — 30 → 60 → 120 s → ceiling after consecutive non-playing responses (`Ok(None)` or `is_playing == false`); resets to 30 s on the next playing track, and also on any tracked 304 Not Modified (an unchanged track — see `not_modified_iteration`). The ceiling is `polling.pause_backoff_max_seconds` (default 300 s, clamped 60–3600 s by `config::clamp_polling`, and floored at the base interval so the ladder cannot invert). At the 30 s default cadence a full day is ~2880 calls; paused it settles at 1/ceiling → ~288-291 calls/24 h at the 300 s default (~72-75 per 6 h) — a ~10× reduction. |
| Profanity filter on outgoing Teams status              | ✅     | `profanity.rs` keeps the curated normalization, leetspeak, separator, Unicode, compound, and boundary rules. The matcher memoizes the full failed state `(text index, word index, stretched, separator-skipped)` for each normalized text/word pair, so overlapping backtracking branches do not re-explore the same subtree; the adversarial 59/60-character leet regression stays clean within 128 explored states. |
| App start minimized (cross-platform) + macOS dock-icon toggle  | ✅     | `config.teams.start_minimized` is read in `src-tauri/src/lib.rs` setup; `window.hide()` runs on **all three platforms** (Windows, macOS, Linux). On macOS only (`#[cfg(target_os = "macos")]`), the same code path additionally calls `tauri::ActivationPolicy::Accessory` to hide the dock icon + menu-bar app menu, making PresenceJam a pure tray-resident app in that case. |
| Per-launch deep-link scheme re-registration           | ✅     | `src-tauri/src/lib.rs` calls `tauri-plugin-deep-link`'s `register_all()` on every launch in the desktop `setup` block. Writes HKCU on Windows, `~/.local/share/applications/presencejam.desktop` + `xdg-mime default` on Linux. On macOS, where that call returns `Err(UnsupportedPlatform)`, the error arm re-claims the scheme through CoreServices (`macos_deeplink.rs::claim` → `LSSetDefaultHandlerForURLScheme`, latched once per process) — see the *macOS deep-link re-claim* row below. (Closes v2.7.0–era deferred item from #66.) |
| App config atomic write                                | ✅     | `src-tauri/src/config.rs::atomic_write_json` — temp-file + fsync + `rename()`. Docs in [`docs/architecture/overview.md`](./architecture/overview.md) (the storage bullet naming `config.rs::save_config` → `atomic_write_json`) and [`docs/architecture/storage-and-config.md`](./architecture/storage-and-config.md).                       |
| Tokens.json atomic write                               | ✅     | `src-tauri/src/token_io.rs::write_tokens_atomic` — same temp-file + fsync + rename pattern.                               |
| Tokens.json encryption (AES-256-GCM, keychain key)     | ✅     | `src-tauri/src/token_io.rs` — on-disk format `b"PJENC" \| version byte (0x01) \| 12-byte random nonce \| AES-256-GCM ciphertext`. The 256-bit key is generated on first use and stored in the OS keychain under `tokens_aes_key:com.presencejam.app` (`keychain.rs::get_or_create_tokens_aes_key`). Plaintext files from ≤ v2.10.0 are migrated to ciphertext on first read. (issue #140) |
| Availability sync (opt-in)                             | ⚠ Partial | `config.teams.availability_sync`, default **off**. While a track plays, `set_teams_presence(...)` against `graph.microsoft.com/v1.0/me/presence/setPresence` (`/users/{oid}` fallback on 404), re-armed at most every 4 min because an `Available` session **times out after 5 min** — a non-configurable timeout distinct from `expirationDuration`, which only bounds the session's absolute life (`PT5M`–`PT4H`, then `Offline`). 4.6 (#636) derives `expirationDuration` from the remaining listening time (`presence_expiration_duration`, `PT4H` for a live/unknown-position stream) and clears the session on quit (`clear_presence_on_exit` from the `RunEvent::Exit` arm). Unit-tested (`test_presence_expiration_duration_is_bounded_by_listening_time`); the arm/clear round trip against a live tenant and the exit-time clear are not observed yet — `clear_presence_on_exit` has no test at all — and on Windows an update-driven quit never reaches it (the installer exits the process without returning). |
| Meeting/call-aware status gating                       | ⚠ Partial | `config.teams.presence_gate`, default **on**. `get_teams_presence` runs before each status write; busy / Do-Not-Disturb / focusing availability or in-a-meeting / in-a-call / presenting activity suppresses the write (`teams.rs::presence_gate_reason`; `focusing` added in #254). 4.6 (#635/#637) adds two more reasons — `respect_manual_status` (default **on**, protects a message the user typed; content-identity authorship check, fails open with no sample) and `gate_when_out_of_office` (default **off**, lowest precedence) — and `presence_gate_decision` is now the single decision function. The Dashboard has specific labels for `quiet-hours`, `track-rule`, `manual-status`, `out of office`, `presenting`, `quiet-time`, and `idle`; `calendar`, presence verdicts, unknown reasons, and the empty reason use the generic label. Truth tables are unit-tested (`test_manual_status_blocks_write_policy`, `test_presence_gate_decision_precedence_and_opt_ins`, `teams.rs::out_of_office_gates_only_when_opted_in`); no test covers the live write-skip against a real tenant, so the row stays ⚠. |
| Tray playback remote                                   | ✅     | `src-tauri/src/tray.rs` + `commands/playback.rs` — Play/Pause toggle, Previous, Next, Shuffle and Repeat toggles (see their own row), a Devices submenu (pick = transfer + start playback there) and an Up Next queue peek (≤3 tracks). Requires **Spotify Premium** (403 otherwise) and the `user-modify-playback-state` scope, which needs a one-time re-auth for pre-3.0 tokens (Settings banner, issue #3.0-P3). Every player action routes through `commands/playback.rs::player_with_refresh_typed` (issue #586) rather than a raw token snapshot. |
| Auto-update (unsigned, GitHub Releases)                | ✅     | `tauri-plugin-updater` is registered in `lib.rs`; the main capability grants `updater:allow-check` and `updater:allow-download-and-install` plus notification permissions. `updater_bg.rs::update_endpoints` selects Stable or the rolling Beta asset before the stable fallback; `check_for_update` resolves the candidate in Rust, the release job hand-assembles `latest.json`, and the separate sign job signs updater payloads independently of OS code signing. `UpdatePrompt.svelte` shows the banner and the channel-appropriate install path. |
| ts-rs generated TS types from Rust wire structs         | ✅     | `src-tauri/src/spotify.rs`, `teams.rs`, `commands/sync.rs`, `config.rs` derive `#[ts_rs::TS]` with `#[ts(export, export_to = "../../src/lib/types-generated/")]`. Cargo test regenerates. See [`docs/architecture/frontend.md`](./architecture/frontend.md#directory-structure).                 |
| Windows / macOS / Linux release matrix builds          | ✅     | `.github/workflows/release.yml` defines a three-way `macos-latest` / `windows-latest` / `ubuntu-latest` matrix: macOS publishes the `.dmg`, Windows publishes `.exe` and `.msi`, and Linux publishes `.deb`, `.rpm`, and `.AppImage`; cite a current release run when claiming a particular published artifact. |
| Local diagnostics page (C5)                            | ✅     | `src-tauri/src/diagnostics.rs::get_diagnostics_snapshot` (async + `spawn_blocking`) — app/Tauri/OS versions, sanitized config, token metadata only (expiry timestamps + presence flags), keychain presence flags, the last failed exit-time update install (#244), and the last 50 log lines after both `redact_sensitive` and `strip_absolute_paths` (the latter reduces POSIX, Windows, and UNC paths to their trailing component). `strip_absolute_paths` also covers the failed-install marker; the config-quarantine fields remain `config_quarantined` + bare-name `config_quarantine_backup` (#537). `Diagnostics.svelte` Copy/Save actions; no network calls. Regression tests assert injected token values never survive serialization. |
| Conditional GET polling (C11)                           | ✅     | `polling/loop.rs` + `polling/poll_once.rs` — ETag stored from `/me/player/currently-playing`, echoed as `If-None-Match`; 304 short-circuits parse/format/filter work. Note: Spotify does not document ETag support on this endpoint; handling is empirical per RFC 9110 and degrades to unconditional GET when no validator is present. |
| Multi-window detach for Logs/Settings (C7)              | ✅     | `src/routes/detached/[pane]/+page.svelte` renders the Logs, Settings, and unknown-pane branches; `src/lib/stores/detach.ts` calls Rust's fixed-table `detach_pane` command, and the main capability no longer grants webview-window creation. Detached labels receive minimal mirrored permissions in `src-tauri/capabilities/detached.json`, including `core:window:allow-close` for Pop back in; a refused close leaves the badge alone. `+layout.svelte` listeners remain window-label-guarded, and the app still boots single-window. |
| Deep-link navigate UX (C2)                              | ✅     | `navigate` event emitted from `handle_deep_link` / Teams auth success (`dashboard` / `settings`); listener in `+page.svelte` defers while Onboarding owns the view. |
| Tray tooltip + native CheckMenuItems + dock badge (C4)   | ✅     | `tray.rs` — live tooltip `Artist — Track (▶|⏸)` on each rebuild; Play/Pause, Shuffle and Repeat as native CheckMenuItems driven by `LAST_PLAYING_STATE` / `LAST_SHUFFLE_STATE` / `LAST_REPEAT_STATE`; macOS-only (`#[cfg]`) presence-gated dock badge wired into the polling loop (`b82f515`). The menu also carries a **Pause / Resume Sync** item (IDs `ID_PAUSE_SYNC` / `ID_RESUME_SYNC`) and, since 4.7.0 (#677), a **Pause sync for…** snooze submenu plus **Resume sync now** while a snooze is active. The tray's own status line is `sync_status_line` ("Syncing — Artist — Track", "Paused — …", "Not syncing", "Syncing — nothing playing"), and the tooltip is `{status_line} · {track_tooltip}`; the words come from the `i18n::Strings` table and a snooze replaces the line with `snooze_status_line` (remaining time + local deadline). |
| Settings dirty-state + clamp feedback + reset (C9)      | ✅     | `Settings.svelte` — unsaved-changes banner via BigInt-safe deep compare; inline clamp feedback mirroring Rust `clamp_polling`; per-section Reset-to-default buttons using `defaultConfig`. |
| Notification throttle + grouping (C8)                   | ✅     | `Dashboard.svelte` — max 1 track-change notification per 5s (throttled tracks don't claim `lastNotifiedId`); replace-in-place via stable id + group tag where the platform supports it. 4.6 moved the opt-in into `src/lib/stores/notifications.ts` (#549); 4.7.0 (#675) replaces it with four config-backed classes (see the notification-classes row below), migrating the legacy `localStorage.notificationsEnabled` value into `track_change` exactly once — and only after the write lands, so a rejected save cannot lose an opt-out. Track changes keep the 5 s throttle (`TRACK_NOTIFICATION_THROTTLE_MS`) and the replace-in-place id. |
| WCAG 2.2 AA accessibility pass (C12)                    | ✅     | Skip link, focus-ring alpha fixes, `prefers-reduced-motion` guards, and darkened status/accent tokens in both themes. The build-version label uses `var(--fg-muted)` at full opacity; `tests/version-contrast.test.ts` mounts the page with production styles, composites effective opacity against the painted background, and requires at least 4.5:1 in both themes. |
| Native menu event IDs are redacted in dispatcher logs (wave-8, #918) | ✅ | `tray::menu_event_id_for_log` preserves known fixed action IDs, reduces payload-bearing IDs to a safe prefix plus suffix length, and reduces unknown IDs to their length. The app-menu unknown branch uses the same helper, with production-seam tests for the formatter and dispatcher record. |
| Main-window capability allowlist + packaged CSP hardening (wave-8, #919/#924) | ✅ | The main-window capability contains only the core/window/event permissions and the updater/notification grants the webview uses; unused global-shortcut and autostart grants are absent. The packaged CSP keeps `base-uri 'self'` and adds `form-action 'none'`, with a config-pinning test for both directives. |
| Typed event payloads + teardown-safe extra listeners (wave-8, #772/#780) | ✅ | The Dashboard consumes `spotify-track-changed` as `TrackInfo`, and the notification helper accepts the same generated type. `useAuthListeners` wraps extra listeners with the same disposed guard as the four auth events, and teardown releases late registrations. |
| Teams write failures distinguish retryable warnings from fatal errors (wave-8, #972) | ✅ | `teams_write_error_policy` classifies rate limits, transient failures, and unexpected responses as warning with `retry_scheduled`; forbidden permission/licence failures are errors with `user_action_required`; expired/dead/invalid-grant failures are errors with `reconnect_required`. The Dashboard renders the retry banner only for a Teams warning carrying `retry_scheduled`. |
| OAuth single-use state binding (C1)                     | ✅     | `pkce::LaunchBinding` binds the SHA-256 of the in-flight verifier at authorize time; `validate_and_consume` take()s the slot after validation so replayed callbacks fail closed (RFC 6749 §10.12 analogue); constant-time compares via `pkce::ct_eq`. Unit tests cover replay/wrong/truncated/malformed states. |
| Silent background update checks (C3a)                   | ✅     | `UpdatePrompt.svelte` re-checks every ~24h; failed silent checks stay console-only. |
| Install-on-quit updates (C3c)                           | ⚠ Partial | `updater_bg.rs` stages a signature-verified payload in memory and applies it on exit. Cancellation is request-scoped: the frontend advances its generation, the backend cancels the exact request, and a completion after cancellation cannot be stored or installed; the published click-to-stage/quit/relaunch smoke is still pending. |
| Release build-provenance attestations (C10)             | ✅ Verified | `release.yml` attests artifacts via SHA-pinned `actions/attest-build-provenance`; exercised live on the v4.0.0 and v4.1.0 tag runs (attest step green both times). `workflow_dispatch` with a `tag` input allows re-cutting an existing `v*` tag. |
| Wave-9 token recovery hardening (#935)                  | ✅     | `token_io.rs::persist_tokens_at_with_retry` keeps blocked ciphertext, transitions a corrupt retry to a writable current-slot snapshot, preserves explicit provider tombstones, and holds both slot guards through snapshot/write. Command and polling clears use `clear_*_if_current` so a replacement wins without re-auth side effects. |
| Wave-9 localized status fallback (#980)                  | ✅     | `commands/misc.rs::preview_status` takes the captured UI locale; `commands/sync.rs` resolves the paused fallback and `polling/poll_once.rs` resolves the stopped fallback through the locale tables, including whitespace-only defaults. `i18n/store.svelte.ts` serializes locale persistence latest-wins; reset keeps canonical default provenance across later locale changes. |
| Wave-9 command adapters (#761)                          | ✅     | Device-code polling, auth event emission, and blocking IO adapters are split into testable production cores; `polling/mod.rs` exposes the Spotify/Teams typed CAS seams. Behavioral tests exercise interval clamping, persistence warnings, and commit ordering. |
| Wave-9 diagnostics and browser coverage                 | ✅     | Diagnostics reports OS release and install flavour; `LogViewer.svelte` exposes a named focusable viewport, and CI/release workflows install Chromium plus WebKit before the browser gate. |
| Dependency prune: shell/store plugins gone (C13)        | ✅     | `package.json`/`package-lock.json` and Cargo lock pruned of `tauri-plugin-shell`/`tauri-plugin-store` + npm shell plugin; ACKNOWLEDGEMENTS rows removed. No imports or capability grants existed. |
| h2 0.4.18                                               | ✅     | RUSTSEC-2026-0258 cleared via lockfile bump (`ed88008`). |
| i18n en/de/fr language picker (C6)                      | ✅ Verified | Shipped in v4.0.0 (`src/lib/i18n/` barrel, `Dict`-typed en/de/fr dictionaries, Settings language picker persisted to `config.locale` — the tray and the native application menu render from the same field, with `localStorage.locale` kept as a pre-paint mirror only, #674). 4.6 adds CLDR plural/number formatting (`Intl.PluralRules` / `Intl.NumberFormat`, built once per locale — French puts `0` in `one`, #616), `<html lang>` retagging on boot and on every switch, and cross-window convergence through a `storage` listener (#620). Wave-8 adds a typed `WEEKDAY_KEYS` registry consumed by Settings, with `tests/i18n.test.ts` pinning all seven weekday keys and including them in literal call-site coverage (#773). Rust-side error strings remain English (documented limitation). |
| Never-re-auth refresh-and-retry (Teams write + playback) | ✅     | `src-tauri/src/polling/poll_once.rs::teams_token_for_write` (call sites in `process_track` and `handle_no_track`) — Teams write `ExpiredToken` does one `refresh_teams_token` + CAS-commit + persist + single retry; `teams-reconnect-required` only on `InvalidGrant`/refresh failure. The no-track clear path mirrors the track-path retry (#455-residual). `src-tauri/src/commands/playback.rs::player_with_refresh` — all six player commands + devices/queue pin refresh through the helper (proactive-plus-one-retry, #464); only `InvalidGrant` surfaces the reconnect message (#428, #367, #375). The tray dispatches Rust-side and matches on the typed error, so it uses the typed core `player_with_refresh_typed` instead (#586). |
| Device-code expiry countdown + one-click fresh code     | ✅     | `src/lib/stores/authFlow.svelte.ts::expiresAtFromResponse` / `formatCountdownMs`; live countdown in `Onboarding.svelte`, `Settings.svelte`, `Reconnect.svelte` with expired state + Get-new-code button; expired codes refused at poll time (#429). |
| Quit-time deferred-update confirmation + stale skip     | ✅     | `src-tauri/src/updater_bg.rs::install_pending_on_exit` skips `staged<=current` with log + marker unless `forced`; `UpdatePrompt.svelte::stageForQuit(force)` confirm surface shows candidate-vs-current with install/skip; stale-skipped state + Install-anyway override; `allowDowngrades: false` in `tauri.conf.json` (#431). |
| Keychain secret-conflict prompt                         | ✅     | `src-tauri/src/config.rs::decide_legacy_secret_outcome` + `migrate_legacy_client_secret_with_app` (both wired in `lib.rs`); one-time `spotify-secret-conflict` event consumed by `Settings.svelte` with reconnect action; plaintext never deleted (#376). |
| Spotify bounded HTTP client + redirect-URI pin          | ✅     | `src-tauri/src/spotify.rs::build_spotify_client` — 10 s timeout + `PresenceJam/<version>` User-Agent on all Spotify calls; `src-tauri/src/commands/spotify_auth.rs::validate_spotify_redirect_uri` exact-match against `presencejam://callback` (#347, #349, #353). |
| Device-code bearer + log-path hygiene                   | ✅     | `src-tauri/src/teams.rs` — device-code error/parse paths are length-only, receipt logs carry `expires_in`/`interval` (#348); `src-tauri/src/diagnostics.rs::tail_log_file` — snapshot statuses carry only `PresenceJam.log`, full path in local logs only (#409). |
| Config schema version + unknown-field retention + corrupt quarantine | ✅     | `src-tauri/src/config.rs` — `schema_version` (default 1) + flattened `extra: BTreeMap` retained across load→save. 4.6 adds the field-level patch merge (`apply_patch` / `update_config`, #535) and the wizard merge (`mergeWizardConfig` writes only the four wizard-owned fields into a clone of the stored config, #531/#542), and pins the quarantine details: a parse failure renames the file to the fixed name `config.json.bak` (never timestamped, rename best-effort and logged), raises `config_was_quarantined()`, and boots on defaults; a *schema* mismatch migrates in place instead (#379). The flag is no longer write-only: `ConfigSummary` carries `config_quarantined` + `config_quarantine_backup` (bare file name, never a path — `quarantine_backup_field`) and `Diagnostics.svelte` shows a dismissible amber **Settings were reset** banner from either fact, so the reset is visible on the first launch *after* the quarantine too (#537, completing #379). |
| Debounced track-change retry (500 ms / 1 s) | ✅     | `polling/poll_once.rs::debounce_active` + `DEBOUNCE_RETRY_SECONDS` — change inside the window parks with no side effects, retry posts exactly once (#364). |
| Identical-status keepalive skip (5 min) | ✅     | `polling/poll_once.rs::should_skip_identical_write` + `last_posted_status` — skips byte-identical writes inside `STATUS_KEEPALIVE_SECONDS`, force-writes after lapse (#384). |
| Mid-track presence-gate re-check | ✅     | `polling/poll_once.rs::gate_recheck_due` on its own `last_gate_check` clock (240 s) — posts late if the gate clears mid-track, fail-safe keeps suppression (#380). |
| Shared Teams token refresh for track + clear | ✅     | `polling/poll_once.rs::teams_token_for_write` — single refresh path for `process_track` and `handle_no_track` (#370). |
| Precise missing-refresh_token error | ✅     | `src-tauri/src/spotify.rs` — exchange without `refresh_token` surfaces `token response omitted refresh_token`, not a generic parse failure (#350). |
| Spotify secret IPC validation (32–512) | ✅     | `src-tauri/src/commands/spotify_auth.rs::validate_spotify_client_secret` — 32-char alphanumeric `client_id` + 32–512 ASCII-alphanumeric secret, both validated at the IPC boundary; manual-code peek-then-take retry + legacy parse-before-keychain (#351, #352, #354, #446). |
| LogViewer render-window + stickiness | ✅     | `LogViewer.svelte` keeps a 500-entry buffer, newest 100 rendered rows, 48 px stickiness, on-disk backfill, and scroll anchoring. The level-badge column is `88px max-content 1fr`, so localized labels size the track instead of overlapping the message; badge typography uses the density font token. `tests/logviewer.test.ts` covers en/de/fr at both densities, and `tests/browser/logviewer.spec.ts` owns the real Chromium layout rectangle assertion. |
| Status rules: quiet hours + track rules | ⚠ Partial | `src-tauri/src/config.rs::StatusRulesConfig` (additive, serde defaults) + `polling/poll_once.rs` rule hooks flowing through the presence-gate path with mid-track re-evaluation; Settings rules card with weekday picker; same-length rule edits flip the change fingerprint (#432). 4.6 (`quiet_hours_active_now` + `rule_gate` hoisted once per iteration) closes the gaps: a quiet window that *opens* mid-track is felt immediately (`quiet_gate_entry_due`, #569), and both clear paths honour rules — the paused clear and the no-track clear, the latter matching only quiet hours or match-all rules since no artist/title exists to scope against (#570). Rules also gained a presence action and a working quiet-hours replacement (see the *Rule-driven presence* and *#538 config fields* rows). Stays ⚠ while the bubble movement itself is unobserved against a live tenant. |
| Pending status posts when presence gate clears mid-track | ✅     | `polling/poll_once.rs` gate re-check branch falls through to the single late-post write with `last_teams_update` untouched; predicate coverage pins the gate-clear path (#430 via #380). |
| Detached windows: theme sync + self-healing badges | ✅     | `app.html` pre-paint bootstrap and the theme store synchronize theme across detached windows; locale storage listeners converge locale too. Zombie detached flags clear with fall-through re-create, and a refused Pop-back-in no longer clears the badge. The detached route's Logs, Settings, and unknown-pane branches are rendered in `tests/about-build.test.ts` (#857). |
| One-click redacted support snapshot from LogViewer | ✅ | `LogViewer.svelte::copySnapshot` sources solely `get_diagnostics_snapshot.recent_logs` (both `redact_sensitive`- and `strip_absolute_paths`-passed) + version/platform; live buffer never pasted; snapshot-copy only, virtualization deferred (#434). |
| Reason-specific Dashboard gate chip | ✅ | `Dashboard.svelte::gatedReasonLabel` maps `quiet-hours`, `track-rule`, `manual-status`, `out of office`, `presenting`, `quiet-time`, and `idle` to their localized strings; `calendar`, every presence verdict (busy / Do Not Disturb / focusing / in a meeting / in a call / presenting), an unknown reason, and the empty reason use the generic `dashboard.presenceGated` label. The empty case is reachable: `gatedReason` is component-local while `gated` lives in the shared store, so a remount with no new event renders the generic line. `tests/dashboard.test.ts` covers the chip's presence/absence, not the per-reason strings. |
| Frontend test harness (Vitest + Playwright)             | ✅     | `vitest.config.js` + `tests/*.test.ts` run in CI via `npm run test:coverage`; `playwright.config.ts` + `tests/browser/*.spec.ts` run via `npm run test:browser`. PR CI and release verification install Chromium and execute the real-browser geometry gate. The contrast regression remains Vitest but evaluates the rendered root page under production CSS in both themes. |
| Multi-platform CI + secret scan + dep audit | ✅     | `ci.yml` macOS + Windows check legs, gitleaks secret-scan, cargo/npm advisory audit; macOS/Windows cfg code compiles in CI (#355, #356, #357). |
| Maintained path/rand crates | ✅     | `dirs` → `directories` 6, `rand` 0.9 with `try_fill_bytes` propagation; single keyring feature set (#418). |
| Status template placeholders ({device}/{playlist}/{progress}/{shuffle}/{repeat}) | ✅ | `src-tauri/src/spotify.rs::format_status_with_context` — one 13-token table (`placeholder_values`) filled per render and substituted in a single left-to-right pass (`substitute_placeholders`), so a value that arrived from Spotify is never re-scanned and re-expanded (#341; `format_status_does_not_expand_data_inserted_tokens`, #580). `{context}` is a literal alias of `{playlist}`; `{shuffle}`/`{repeat}` are icon-only (🔀/🔁 or `""`); `{progress}` is `M:SS` with no hour rollover (`90:00` for a 90-minute episode) and **empty** when `progress_ms` is `None` (#165). UI hint keys `settings.placeholdersHint` / `onboarding.placeholdersHint` list the music tokens. |
| Podcast & audiobook episodes | ✅ | `src-tauri/src/spotify.rs::map_media_item` reads the item's own `type` against the documented `oneOf(track, episode)` union: an episode's `show.name` takes the `artist` slot and `show.publisher` the `album` slot, and `EpisodeInfo` carries `show_name`/`publisher`. `poll_once::process_track` selects `DEFAULT_EPISODE_STATUS_FORMAT` (`🎙️ {show} - {episode}`) whenever `now.episode.is_some()`, so a music template is never applied to an episode (#581). Adverts are unchanged: the envelope's `currently_playing_type` is gated and `Ad | Unknown` returns `None` from the mapper — "nothing playing" (#161). The queue mapper feeds episodes into tray *Up Next* and drops ads (#583). Note: the episode template is a built-in constant — there is **no** `teams.episode_status_format` config key. |
| Tray Shuffle / Repeat toggles | ✅ | `src-tauri/src/tray.rs` — both are native CheckMenuItems whose marks read `LAST_SHUFFLE_STATE` / `LAST_REPEAT_STATE`, written by `note_playback_modes` from the poll body (no extra request) and optimistically on a successful tray toggle before `force_tray_refresh`. Shuffle targets the inverse of the last known state; Repeat targets `RepeatState::next()`, i.e. `off → context → track → off`, and its label spells the mode out because a check mark cannot distinguish the last two (#582). A rejected command records nothing, so the tray never claims a state Spotify refused; the error surfaces on `playback-error` as an in-app toast. |
| Live stage progress + cancel for install-on-quit | ✅ | `updater_bg.rs` binds progress, completion, and cancellation to the exact stage request id and generation. Cancel invalidates an in-flight completion, removes an already-committed payload, or tombstones cancellation that arrives before staging begins; a losing download cannot stage or install, and queued terminal events are suppressed. |
| Keychain-unavailable surface (locked vs absent) | ✅ | `keychain.rs::KeychainPresence` preserves `Present` / `Absent` / `Unavailable(help)`. Config loads stamp both derived fields through `with_keychain_flags` and the warm presence cache; a fresh `Present` observation is reused for 30 seconds, while a cold or expired cache falls back to the direct namespaced/legacy probe. Explicit user-action checks remain uncached. |
| Log viewer on-disk backfill | ✅ | `src-tauri/src/commands/logs.rs::get_recent_logs` reads the tail on `spawn_blocking`, clamped twice — `MAX_LOG_LINES` 500 and `LOG_TAIL_MAX_BYTES` 256 KiB — dropping the partial first line; a missing file is `Ok(empty)`, not an error. `LogViewer.svelte` registers `log://log` first and then awaits the seed, prepending it and re-clamping to `MAX_BUFFER`. The seed is **raw, not redacted** by explicit decision (#595): redaction belongs to the paste-able snapshot path. **Clear** sets `seedCancelled` so history cannot reappear. |
| macOS deep-link re-claim via CoreServices | ✅ | `src-tauri/src/macos_deeplink.rs::claim` → `LSSetDefaultHandlerForURLScheme` (CoreServices via `objc2-core-services`), reached from the error arm of `register_all()` in `lib.rs`'s desktop `setup` block — the arm macOS always takes (`Err(UnsupportedPlatform)`). The scheme list is read from `tauri.conf.json` rather than hardcoded, the call is latched once per process (`CLAIMED`, consumed on the attempt), and failure is only logged (expected under `tauri dev`, where the process is not a bundle: `kLSNotAnApplicationErr`) (#66, #628). |
| Release pipeline: tag/version gate + tagged-commit verify | ✅ | `.github/workflows/release.yml` — the `resolve-tag` job validates the tag exists *and* that `tauri.conf.json`, `package.json` and `Cargo.toml` agree with it, since a drifting tag makes `latest.json` advertise a version the app never becomes (permanent re-offer loop) (#605). A separate `verify` job checks out the resolved tag and reruns the tagged commit's Linux gate set, including Rust tests, `npm run check`, Vitest coverage, and the Chromium Playwright gate; `build:` declares `needs: [resolve-tag, verify]` so tag pushes — which never trigger `ci.yml` — can no longer publish unverified code (#606). `ci.yml` carries the PR-time version twin. |
| Polling failure classification (dead credentials vs network) | ✅ | `polling/loop.rs` + `poll_once.rs` — the poll loop counts `SourceError::Auth` as its auth/reconnect path. Spotify's `sources/spotify.rs` maps `ExpiredToken`, `InvalidGrant`, and `NotPremium` into that path; all non-Auth source variants — including `SourceError::Unauthenticated`, `SourceError::Transient`, and `SourceError::Other` — feed a separate `consecutive_network_failures` counter with its own `NETWORK_FAILURE_THRESHOLD` (12) and capped backoff (`NETWORK_BACKOFF_CAP_SECONDS` 300), so a flaky network can no longer stop the session or pop an OAuth window (#568). `record_success` resets both counters. |
| Presence preview survives a Dashboard remount | ✅ | `src/lib/stores/presence.ts` — a module-level store holding the posted status / gate flag / availability; `+page.svelte` destroys the view component on every view switch, so component-local `$state` used to reset the preview to "Not configured". `Dashboard.svelte` derives `statusPreview` from `$presence.postedStatus` and hydrates from the event stream (#547). 4.7.0 (#670) closes the relaunch half too: `SyncStatus` (`commands/sync.rs`) carries `last_posted_status` / `presence_gated` / `presence_paused`, the listeners moved out of the component into the always-mounted `routes/+layout.svelte`, and `Dashboard.svelte` hydrates `stores/presence.ts::hydrate` from `get_sync_status` on mount (guarded by a snapshot revision), so a status, gate or pause that lands while another view is mounted is no longer lost — and a pause shows a paused track card rather than "Nothing playing". |
| Config quarantine warning on the Diagnostics page | ✅ | `src-tauri/src/diagnostics.rs::ConfigQuarantine::observe` reads `config::config_was_quarantined()` (this process did the quarantine) plus an existence probe for the `.bak` sibling, and injects both into `ConfigSummary` at the command boundary, so `build_snapshot` stays drivable from a test with a planted quarantine. `Diagnostics.svelte` renders the amber banner from *either* fact — the per-process flag alone would disappear on the first restart, which is exactly when the user notices their settings are gone — names the backup file (`diagnostics.quarantineBackupPresent`, or the missing-backup wording), says which folder it lives in (no absolute path), and is dismissible per session without invoking anything, because the backup is the only copy of the lost settings (#537). Driven by `tests/diagnostics-quarantine.test.ts`. |
| Rule-driven presence (quiet hours / track rules set availability) | ⚠ Partial | `config.rs::PRESENCE_COMBINATIONS` is the closed set of five pairs Graph accepts — Available/Available, Busy/InACall, Busy/InAConferenceCall, Away/Away, DoNotDisturb/Presenting — and `normalize_presence_pair` is the only constructor, so an unsupported or half-filled pair is cleared rather than sent. `poll_once::rule_gate_at` returns a `RuleDecision { reason, replacement, presence }`: **quiet hours take strict precedence** (an early return — a matching track rule is never evaluated), an empty replacement means suppress, and `rule_presence_backoff` arms the rule's pair on the paths that return before the shared availability block, so a suppression-only rule still moves the bubble; it is **inert unless `availability_sync` is on**. `arm_presence_session` is the single `setPresence` call site and `should_arm_presence` arms immediately on a changed pair. Unit-tested (`test_rule_actions_suppress_replace_and_set_presence`, `test_should_arm_presence_switches_immediately_and_keeps_cadence`, `config.rs::test_normalize_presence_pair_accepts_only_documented_combinations`); the bubble movement itself needs a live Teams tenant, so ⚠. |
| Manual-status respect (never overwrite a hand-set status) | ⚠ Partial | `poll_once::manual_status_blocks_write` — blocks only when `teams.respect_manual_status` is on (default **on**), a presence sample exists, `statusMessage.message.content` is non-empty, its expiry has not lapsed, and the text is not byte-identical (after trim) to `last_posted_status` / `last_posted_placeholder`. Authorship is decided by **content identity**; `publishedDateTime` is deliberately not modelled. No sample (a failed read) **fails open** — the write proceeds — and the guard protects the status message, not the presence session. The gated branch returns `playing_track_sleep(...)`, so the loop keeps polling and only the Teams write is skipped; the chip reads "you set a status message by hand". Truth table unit-tested (`test_manual_status_blocks_write_policy`, `test_presence_gate_decision_precedence_and_opt_ins`); end-to-end against a live tenant not observed. |
| Out-of-office gating (opt-in) | ✅ | `config.teams.gate_when_out_of_office`, default **off** (`default_gate_when_out_of_office`). Fires on either documented signal — `outOfOfficeSettings.isOutOfOffice` or an `outOfOffice` activity (case-insensitive; a body omitting the object parses as not-out-of-office) — and is the **lowest-precedence** reason, so busy/in-a-call wins the explanation. A rule carrying its own presence pair overrides it for that iteration (`ooo_gate_enabled(config, rule.presence.is_some())`). Reason string is the literal `out of office` (spaces). Unit-tested (`teams.rs::out_of_office_gates_only_when_opted_in`, `parse_presence_body_carries_status_message_and_ooo`). |
| Bounded Available session + quit clear | ✅ | `poll_once::presence_expiration_duration` derives the requested `expirationDuration` from the remaining listening time plus one re-arm period, clamped to Microsoft's documented `PT5M`–`PT4H`; a live/unknown-position stream (issue #165) keeps `PT4H`. `lib.rs`'s `RunEvent::Exit` arm runs `updater_bg::install_pending_on_exit` **first**, then `polling::clear_presence_on_exit`, which is best-effort and log-only, bounded by `teams::EXIT_CLEANUP_TIMEOUT` (3 s), no-ops when nothing of ours was armed or posted, skips an expired stored token, and does not emit `presence-availability-updated`. Unit-tested for the pure duration function; the exit-time clear now decides from the `ExitSnapshot` written on every posting/arming iteration (`polling/state.rs`), so the loop's exit-tail `reset_write_clocks()` can no longer cancel it (#684), and both the behaviour and the ordering are pinned by `test_exit_cleanup_survives_the_loop_exit_tail` and `test_clear_presence_on_exit_reads_the_snapshot_not_the_clocks` (a Windows update-driven quit never reaches the clear). |
| Three config fields exposed in 4.6 (#538) | ✅     | All three are editable in Settings with clamp feedback and all three are consumed. `status_rules.quiet_hours[].replacement_status` — posted from the playing path and the no-track clear, capped at `MAX_RULE_STATUS_CHARS` (160); it is part of the change fingerprint, so an edit mid-track forces one rewrite. `teams.profanity_extra_words` — `profanity::filter_status(text, placeholder, is_playing, extra_words)` runs the entries through `contains_extra_word` against the same normalized text with the built-in evasion machinery (separator skip/wildcard, leet, stretch gates, the #578 span check) but without the built-in stem carve-outs, so a user's `not` does not flag `noting`; consumed by the polling status write and by `preview_status`. `polling.pause_backoff_max_seconds` — the pause-ladder ceiling (see the backoff row above). Settings hints (`settings.extraWordsHint`, `settings.pauseBackoffClampHint`) match the implemented behaviour. |
| Tray snooze — "Pause sync for…" (v4.7.0)                | ✅     | `src-tauri/src/tray.rs` — the snooze submenu (`ID_SNOOZE_30` / `ID_SNOOZE_1H` / `ID_SNOOZE_TOMORROW`, plus `ID_SNOOZE_RESUME` while one is active) writes `config.rs::AppConfig.snooze_until` as an RFC3339 UTC instant, and a deadline that passed while the app was closed is cleared by `tray.rs::clear_expired_snooze_at_startup` (run from the tray setup), and one that lapses while the app is running by `poll_once.rs::clear_snooze_if_expired` on the iteration that observes it — both through `config.rs::clamp_snooze`, which every save also runs via `config.rs::clamped_config`; `config.rs::load_config` deliberately only reports the state (`config.rs::snooze_expired_deadline`) because it is a reader holding no write guard. `poll_once.rs::snooze_gate` runs **before** the write clocks are loaded, so a snoozed iteration performs no Spotify or Graph request, touches no clock and leaves the existing Teams status alone; the driver sleeps at `polling.max_interval_seconds` and re-evaluates the deadline every iteration (the thread is never stopped or parked), and `run_oneshot` honours the same gate so a CLI one-shot cannot undo a snooze. "Until tomorrow" is the next **local** midnight (`config.rs::snooze_preset_deadline`, never `now + 24 h`). `Dashboard.svelte` shows the countdown chip with **Resume now**. |
| Global shortcuts (v4.7.0)                               | ✅     | `src-tauri/src/commands/shortcuts.rs` — two slots (`ShortcutSlot::TogglePlayback` / `ToggleSync`) with defaults `CmdOrCtrl+Alt+P` / `CmdOrCtrl+Alt+S` stored in `config.rs::ShortcutsConfig`; `plan_shortcuts` decides each slot independently, `validate_accelerator` refuses an unparsable, colliding or modifier-less combination (bare keys need F1–F24 or media, #810), and `apply_plan` registers through `tauri-plugin-global-shortcut` (`lib.rs`). Registration is per slot and never fatal: a refused grab (a Wayland compositor, or a combination another app owns) is recorded as that slot's status and shown in Settings as `settings.shortcutRegistrationFailed` with the reason, while the other slot keeps working. `dispatch_event` routes playback thr… |
| CLI flags (v4.7.0)                                      | ✅     | `src-tauri/src/lib.rs` — `--status` (`CliCommand::Status`, prints the `commands/sync.rs::SyncStatus` JSON and exits 0), `--sync-once` (exactly one `polling::run_oneshot` iteration; exit 0 on completion, 1 with the poller's reason on stderr), `--help` (usage text, exit 0). `--status` and `--help` are handled before any Tauri app is built, so they need no window, no tray icon and no single-instance lock; `--sync-once` drives the app's own poller and therefore needs a display server on Linux (documented `xvfb-run` workaround). Unknown arguments are ignored and launch the GUI exactly as before, like `--minimized` and a `presencejam://` URL. On Windows release builds the runner attaches to the parent console before CLI output (#818); a `windows-cli-smoke` CI job asserts `--help` via `cmd /c`. |
| Update channel (v4.7.0)                                 | ✅     | `config.rs::UpdatesConfig.channel` (`UpdateChannel::{Stable, Beta}`, serialized lowercase, lenient on an unknown value → `Stable` with a log) selects the endpoint list in `updater_bg.rs::update_endpoints`: Stable is `…/releases/latest/download/latest.json`; Beta is the rolling `…/releases/download/beta/latest-beta.json` asset **followed by** the stable URL, so a missing or non-newer beta manifest falls through to stable. `updater_bg.rs::check_for_update` resolves the banner's candidate in Rust, and on Beta the immediate download-and-relaunch path is replaced by install-on-quit. |
| Log rotation + retention (v4.7.0)                       | ✅     | `lib.rs::log_rotation_strategy` maps a clamped `logging.keep_files` to `tauri_plugin_log::RotationStrategy::KeepSome(n)` — including at 1, because `KeepOne` never prunes the dated archives — and the file target sets `.max_file_size(max_file_size_mb * 1024 * 1024)`. `config.rs::clamp_logging` bounds `max_file_size_mb` to 1–500 (default 10) and `keep_files` to 1–20 (default 3). `keep_files` counts **archived** files only: the live log is kept in addition, so the folder holds at most `keep_files + 1` files, which is what the Settings → Logging card states beside the field. The card also carries `enabled`, `log_level` and **Open logs folder**; size and retention apply from the next launch, level and on/off immediately. |
| Rule scheduling, rule priority, quiet-hours pause-polling and editable pause/stop texts (v4.7.0) | ✅ | `config.rs::TrackRuleEntry` gains `days` (ISO 1=Mon…7=Sun, empty = every day), `start_minutes` and `end_minutes` (defaults `[]`, `0`, `1440`), normalized by `config.rs::clamp_track_rule_window`; `poll_once.rs::rule_gate_at` walks `status_rules.track_rules` **in array order and returns the first match** (the Settings card's move-up / move-down buttons edit that order), while `quiet_hours` keep strict precedence. Midnight-crossing windows own the night they start (#794): each half is tested against its own day, so Monday 22:00–07:00 covers Monday night into Tuesday morning. `config.rs::QuietHoursEntry.pause_polling` (default `false`) makes an active window skip the iteration entirely — no Spotify/Teams request, no clock touched, thread never parked — logged once per transition (`[POLLING] quiet hours: polling paused u… |
| Settings export / import (v4.7.0)                       | ✅     | Import validates the selected document and rejects plaintext `client_secret`, then stages it beside `config.json` in an app-owned `.import.tmp` sidecar with exclusive creation and fsync. The live file moves to `config.json.bak`, the staged file is atomically installed, and a failed install rolls back. Replacement, reload/validation, and `AppState` adoption run under one config write guard. |
| Follow-system theme + compact density (v4.7.0)          | ✅     | `src/lib/stores/theme.ts` — `Theme = 'dark' \| 'light' \| 'system'` with `resolveTheme` and a `matchMedia('(prefers-color-scheme: light)')` `change` listener that repaints only while the preference is `system` (an explicit dark/light is never overridden by the OS); `src/app.html`'s pre-paint bootstrap resolves the stored preference too, so a System user sees no flash of the wrong theme. A sibling `density` store sets `html[data-density="compact"]`, a token-scale override block in `src/app.css` applied before first paint, with no component variants; Settings → Appearance exposes the third theme card, the hint, and the density toggle. |
| Tray / native-menu localization (v4.7.0)                | ✅     | `i18n.rs` keeps the en/de/fr native strings in one table and the production literal scanner lexes double-quoted and raw URL-containing literals in `tray.rs` and `menu.rs`, ignoring comments and char syntax while failing on copy absent from the explicit allowlist. Scanner self-tests cover translated copy, field references, comments, URL punctuation, and char/lifetime boundaries. |
| Frontend coverage ratchet (v4.7.0) + Rust coverage gate (wave-6, #838)   | ✅     | `vitest.config.js` sets `coverage.provider: 'v8'` with thresholds equal to the measured baseline (statements 68.91, branches 62.57, functions 73.33, lines 68.02) and an explicit `exclude` for the generated `src/lib/types-generated/**` and the two files the provider cannot instrument — so the local and CI denominators match. `ci.yml`'s `frontend` job runs `npm run test:coverage` in place of `npm test`, and a dropped percentage fails the job. Wave-6 adds a `rust-coverage` job (`cargo llvm-cov --lib`, lcov artifact, per-file floors for `poll_once.rs`/`config.rs`/`token_io.rs` starting at 0 with calibration TODO). Rust coverage tooling no longer out of scope. |
| Dependency licence + source gate (wave-6, #776) + npm prod-audit gate (#844) | ✅ | `src-tauri/deny.toml` allow-lists the tree's licences with `[sources] unknown-registry/unknown-git = "deny"`; `ci.yml`'s SHA-pinned `cargo-deny` job enforces `licenses` + `sources`. The `dep-audit` job gates the npm production tree (`npm audit --omit=dev --audit-level=high`) with step-scoped `continue-on-error` on the advisory legs; the `ignore:` list carries owner + expiry tied to #642. |
| Docs link audit in CI (wave-6, #831) + vendored-binary guard (#753)      | ✅     | `ci.yml`'s `docs-links` job runs stdlib-only `docs/link-audit.py` (relative targets + `#anchor` resolution); `no-vendored-binaries` fails on untracked, unignored root paths (chrome-headless-shell stays gitignored). Both documented in `docs/RELEASING.md` §3 + §5. |
| Rust tests on macOS leg; Windows check-only (wave-6, #836)               | ⚠ Partial | `ci.yml`'s `rust-platform-check` executes `cargo test --all-targets` on `macos-latest` (`if: macos-latest`); the Windows lib test binary aborts at loader on the current image (`STATUS_ENTRYPOINT_NOT_FOUND`), so Windows keeps the `cargo check` compile gate until the image links cleanly. |
| AGENTS.md single agent contract (wave-7)                                | ✅     | `AGENTS.md` is the canonical file every AI coding agent reads (Claude Code, Codex, Cursor, aider, OpenClaw, omp). `CLAUDE.md` is reduced to a redirect stub. Toolchain pins, gate set, authoring rules for Rust + Svelte, i18n / auth / storage contracts, CSP + capability allowlist, Conventional Commits + two-reviewer 100/100 PR convention, CI gate map, must-NOT list, per-issue recipe, and symbol glossary all live in one place. |
| Native form-control theming follows the painted theme (wave-7, #959)    | ✅     | `src/app.css` declares `color-scheme: dark` on `[data-theme='dark']` and `color-scheme: light` on `[data-theme='light']`; `+layout.svelte` drives the `<meta name="color-scheme">` from `appliedTheme`, so native scrollbars / popups match the app's painted theme rather than the OS preference. |
| German tray ↔ webview terminology aligned (wave-7, #901)              | ✅     | Seven Rust tray/menu German strings (open-logs-folder, status-syncing and its no-track sibling, pause-sync, resume-sync, manual-status-clear, profile-empty) match the webview's nouns. German was the only locale that named the same control or state two different ways in adjacent UI; en/fr were already aligned. |
| Update banner wired to the token system (wave-7, #902)                  | ✅     | Six dead tokens (`--r-xl`, `--ease-in-out`, `--z-banner`, `--partner`, `--partner-soft`, `--bg-overlay`) and two dead classes (`button.ghost` / `.btn-ghost`, `.text-muted`) are removed; the banner's `z-index` + `box-shadow` are now `var(--z-update-banner)` + `var(--shadow-update-banner)`. |
| Logger flushes before the build-failure exit (wave-7, #947)             | ✅     | `lib.rs::run` `Builder::build` failure arm calls `log::logger().flush()` before `std::process::exit(1)`, mirroring the CLI failure path. tauri-plugin-log's file target buffers, so the "the app will not start" diagnostic can no longer be lost. |
| Stale comment citations swept (wave-7, #856, #908)                      | ✅     | `polling.rs` / `commands.rs` / `ts_rs_export` references and nine other stale `file:line` citations across the codebase point at live homes. `grep -rn 'polling\.rs\|commands\.rs' src-tauri/src` returns no matches. |

| Desktop notification classes (v4.7.0)                   | ✅     | `config.rs::NotificationsConfig` (`AppConfig.notifications`): `track_change` (dispatched from `Dashboard.svelte`, 5 s throttle + replace-in-place id), `sync_stopped` (from the always-mounted `routes/+layout.svelte`, and only for `payload?.self_terminated === true`, so a Pause Sync you clicked stays quiet), `auth_required` (from `teams-reconnect-required` unless `payload?.user_initiated === true` — the one user-initiated emitter is `commands/onboarding.rs::reconnect_teams`) and `update_staged` (from `update-stage-complete { version }`, emitted once per successful stage by `updater_bg.rs`). All four default on and each has its own Settings toggle; `stores/notifications.ts` also mirrors preferences across windows. |

## Release smoke (run once against the published release)

The two release-smoke rows are **Install-on-quit updates (C3c)** and
**Sign-in survives a long idle period**. They remain ⚠ Partial until the
published-artifact click-to-stage/relaunch flow and the real long-idle
credential flow have been observed. Other ⚠ Partial rows remain partial for
tenant, Graph, platform, or CI-runtime reasons; do not flip them from a
published-release smoke run.

### 1. Install-on-quit updates (C3c)

The smoke must start from the **previously published** build:
`allowDowngrades: false` (`tauri.conf.json`) plus the stale-stage guard
(`updater_bg.rs::install_pending_on_exit` skips `staged <= current` unless
`forced`) mean the running build can never stage itself.

```bash
# 1. Install the previous published build (e.g. PresenceJam-v4.5.2.msi /
#    PresenceJam-macos.dmg / PresenceJam-linux-amd64.AppImage) and launch it.
# 2. Tail the log while the startup update check runs:
tail -f ~/Library/Logs/com.presencejam.app/PresenceJam.log                 # macOS
tail -f ~/.local/share/com.presencejam.app/logs/PresenceJam.log           # Linux
# PowerShell: Get-Content -Wait "$env:LOCALAPPDATA\com.presencejam.app\logs\PresenceJam.log"
```

3. When the banner shows *Update vX.Y.Z available*, pick **Install on quit** and
   confirm. Expect `[UPDATER.BG] stage_deferred_update: SUCCESS`.
4. Tray → **Quit**. Expect, in order:

```
[UPDATER.BG] install_pending_on_exit: installing vX.Y.Z on quit
[UPDATER.BG] install_pending_on_exit: vX.Y.Z installed; takes effect on next launch (Windows installer relaunches automatically)
```

5. Relaunch (Windows does it for you) and check About → the new version.
   Record per platform whether the app relaunched itself or needed the manual
   relaunch — "installed, but the old UI is up until the next launch" is the
   documented macOS/Linux behaviour, not a failure.

**Flips:** *Install-on-quit updates (C3c)* → ✅, annotated with the release tag
and the three platform results.

### 2. Sign-in survives a long idle period

The condition cannot be seeded — `tokens.json` is AES-256-GCM ciphertext
(`token_io.rs`), so an expired `expires_at` cannot be hand-written. The
wall-clock wait **is** the driver.

1. Sign in to both providers on the new build, confirm a status posts, quit.
2. Leave the app closed for **more than 1 hour** (the access-token lifetime).
3. Relaunch. Expect the **Dashboard**, not the wizard, and in the log:

```
[CMD.ONBOARDING] is_onboarding_complete: spotify session refreshed (access token was expired at launch)
[CMD.ONBOARDING] is_onboarding_complete: teams session refreshed (access token was expired at launch)
[CMD.ONBOARDING] is_onboarding_complete: result=true (spotify_configured=true, spotify_valid=true, teams_configured=true, teams_valid=true)
```

Record the platform and the actual wall-clock gap beside the result.

**Flips:** *Sign-in survives a long idle period* → ✅. If the wait cannot be
run, the honest alternative is to leave the row ⚠ and reword it to
"⚠ Partial — decision table unit-tested (`onboarding.rs::session_verdict`);
end-to-end relaunch smoke pending" rather than promoting it on a unit test.

> The macOS deep-link re-claim defence is already ✅ and is not a release-smoke
> item: launch re-claims the scheme through CoreServices, while the remaining
> between-launch-and-callback risk is covered by the PKCE/state binding.

### 4.7.0 smoke record (2026-09-17)

Run against the **published** `v4.7.0` release, on a headless Linux host
(`XDG_SESSION_TYPE=tty`, no Secret Service, no Spotify/Teams credentials). Both
rows above therefore stay ⚠, per the rule stated above them.

**Verified from the published artifacts:**

- `releases/latest/download/latest.json` serves `version: 4.7.0` with the three
  platform keys (`darwin-aarch64`, `windows-x86_64`, `linux-x86_64`) and
  non-empty minisign signature bodies.
- All three platform asset URLs return **200**, and `SHA256SUMS.txt` is present
  with eight entries.
- The published Linux AppImage boots and logs
  `[APP] setup: PresenceJam 4.7.0 started successfully`, with the tray and the
  application menu initialized — so the shipped binary self-reports the version
  the manifest advertises (the mismatch that would re-offer an update forever).
- `presencejam --status` on the shipped binary prints the seven-field
  `SyncStatus` JSON — including 4.7.0's new `last_posted_status`,
  `presence_gated` and `presence_paused` — and exits 0.
- `presencejam --sync-once` on the shipped binary exits **1** with the reason on
  stderr (`no Spotify client_id configured …`), confirming the 4.7.0 exit-code
  fix in the released build.

**Not run (needs a desktop session with real credentials):**

- the click-to-stage leg of smoke 1 (banner → *Install on quit* → quit →
  relaunch). A headless 4.6.0 run cannot produce it: the staging is driven by a
  click on the banner, and a 4.6.0 startup log contains no check outcome.
- all of smoke 2, which requires signing in to both providers and leaving the
  app closed for more than an hour.

Both rows are to be flipped by a maintainer who runs those steps; the wording
added to each row records exactly what remains.

## Documented gaps (do work; deliberately out of scope for the version tested)

| Area                                               | Status | Notes                                                                                                                                  |
|----------------------------------------------------|--------|----------------------------------------------------------------------------------------------------------------------------------------|
| Spotify **Free** (non-Premium) users                | ❌     | Spotify's Web API requires a Premium subscription for `/me/player/currently-playing`. This is a Spotify platform restriction — PresenceJam cannot work around it. Don't try to install without Premium. |
| macOS deep-link hijack defence (full)              | ✅     | `tauri-plugin-deep-link`'s `register()` returns `Err(UnsupportedPlatform)` on macOS, but that error arm is now load-bearing: `macos_deeplink::claim` issues CoreServices' `LSSetDefaultHandlerForURLScheme`, which writes the user's *preferred* handler and so overrides LaunchServices' first-come-first-served rule (`tauri.conf.json` scheme added → re-claimed automatically; latched once per process). 4.6, #66/#628. Failure is logged only and the #65 PKCE launch-binding defence stands in. The residual gap is an attacker registering between our launch and the callback. |
| `digest-mismatch: error`                           | ✅     | v3.2.0 sets `digest-mismatch: error` in both `actions/download-artifact` invocations (v8 default is error; explicit error is correct). Flipped from `warn` after one clean release cycle v2.8.0 → v3.2.0. |
| Code signing & notarization (Windows + macOS)      | ⚠ Not signed | Binary downloads are unsigned (or unsigned-plus-AppleGatekeeper Bypass for macOS). The README documents the `Right-click → Open` workaround for macOS. Microsoft SmartScreen will warn on first install of an unsigned `.msi`. **v3.0's auto-update works without OS signing**: the update payload is signed separately (minisign) and verified against the pubkey baked into the app, so updated builds keep the same unsigned/Gatekeeper story. |

## Have not been verified end-to-end (Verify with maintainer before promising to a user)

| Area                                                         | Status | Why this needs explicit verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
|--------------------------------------------------------------|--------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Microsoft 365 GCC / DoD / EduTenants / China tenants**      | ⚠ Verify | The Graph presence APIs are **not supported** for personal Microsoft accounts, so PresenceJam requires a work or school account. For work/school tenants, `Presence.ReadWrite` (delegated) and `offline_access` are both `AdminConsentRequired: No` per the Graph permissions reference; tenants that disable or restrict user consent (Entra user-consent settings) can route any delegated permission — these included — through admin approval. The China (21Vianet) national cloud does not support `setStatusMessage` at all. If you've tested PresenceJam against a tenant-restricted M365 environment, please file an issue or PR against this file with the result — the maintainer has only verified work/school accounts in the public cloud. To check from your end: open `https://developer.microsoft.com/en-us/graph/graph-explorer`, sign in with the relevant account, and confirm `GET /me/presence` returns 200. If it returns 403/401, that tenant won't work out-of-the-box. |
| **Tauri framework future major (3.0) compatibility**  | ⚠ Verify | The manifest deliberately constrains Tauri to a narrow compatibility range; re-run the tray, deep-link, and activation-policy checks whenever that range changes, and revisit this row when Tauri 3 stabilizes. |
| **Linux distributions other than ubuntu-latest / Fedora / Arch** | ⚠ Verify | The CI matrix only tests `ubuntu-latest`. Distros with non-glibc libc (Alpine, Void), unusual default secret-service setups (Pantheon without gnome-keyring), or Wayland-only desktops may hit edge cases (Tray icon visibility on Wayland is a known ecosystem issue, not specific to PresenceJam). See `SETUP.md#linux-keyring` for the Secret Service requirement that all distros must meet. |
| **`actions/download-artifact` v8 checksum-drift behavior in production** | ✅ Verified | v3.2.0 sets `digest-mismatch: error` (was `warn` in v2.8.0–v3.1.x). No checksum-drift events observed in production; strict mode now enforced. |

## Explicitly out of scope (will not be added in any foreseeable release)

- **Internet-archive / fallback authentication.** PresenceJam is local-first; there is no remote auth proxy.
- **Custom Teams status emoji.** Teams' own `setStatusMessage` API takes one text message, so there is no per-track emoji surface. The emoji the app posts are fixed (🎵 / 🎙️, ⏸️ in the preview), but the **pause and stop texts themselves are user-editable since 4.7.0** (`teams.paused_status_format` / `teams.stopped_status_format`, Settings → Status rules).
- **Lyrics / non-Now-Playing Spotify data.** The Web API surfaces what's documented; we don't scrape unofficial endpoints.
- **Slack / Discord / other IM status sync.** Out of scope — different platforms with different SDKs; would be a separate project.

## Reporting a stale row

If you find an entry here that doesn't match the code on `main`, open a
PR against this file with the correction + a link to the verified
source (a code reference, doc reference, or a CI run URL). One-row
changes are an ideal first contribution.
